# rust-rf examples

Open these notebooks with Jupyter and select the **Rust** kernel supplied by
`evcxr_jupyter`. Every notebook declares a relative path dependency on the local
rust-rf checkout and leaves outputs untracked.
